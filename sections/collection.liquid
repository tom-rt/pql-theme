<div class="main-wrapper">
  <div class="main-column pt">
    {{ collection.title }}
    {% for product in collection.products %}
        <!-- product info -->
        {{ product.title }}
    {% endfor %}

  </div>
</div>

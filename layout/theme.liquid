<!DOCTYPE html>
<html class="html">
  <head>
    {{ content_for_header }}
    {{ 'custom-fonts.css' | asset_url | stylesheet_tag }}
    {{ 'base.css' | asset_url | stylesheet_tag }}

  </head>

  <body class="body">
    {%- comment -%}
      {% section 'topbar' %}
    {%- endcomment -%}
    <div class="main-mt">
      {{ content_for_layout }}
    </div>
  </body>
</html>
